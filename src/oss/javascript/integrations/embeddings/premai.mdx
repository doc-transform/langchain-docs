---
title: "Prem AI 集成"
description: "使用 LangChain JavaScript 集成 Prem AI 嵌入模型。"
---

`PremEmbeddings` 类使用 Prem AI API 为给定文本生成嵌入。

## 设置

要使用 Prem API，你需要一个 API 密钥。你可以在[这里](https://studio.premai.io/sign-up)注册 Prem 账户并创建 API 密钥。

你需要先安装 [`@langchain/community`](https://www.npmjs.com/package/@langchain/community) 包：

<Tip>
See [this section for general instructions on installing LangChain packages](/oss/langchain/install).
</Tip>

```bash npm
npm install @langchain/community @langchain/core
```

## 用法

```typescript
import { PremEmbeddings } from "@langchain/community/embeddings/premai";

const embeddings = new PremEmbeddings({
  // In Node.js defaults to process.env.PREM_API_KEY
  apiKey: "YOUR-API-KEY",
  // In Node.js defaults to process.env.PREM_PROJECT_ID
  project_id: "YOUR-PROJECT_ID",
  model: "@cf/baai/bge-small-en-v1.5", // The model to generate the embeddings
});

const res = await embeddings.embedQuery(
  "What would be a good company name a company that makes colorful socks?"
);
console.log({ res });
```

## 相关链接

- 嵌入模型[概念指南](/oss/integrations/text_embedding)
- 嵌入模型[操作指南](/oss/integrations/text_embedding)
