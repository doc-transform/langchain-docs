---
title: "腾讯混元集成"
description: "使用 LangChain JavaScript 集成腾讯混元嵌入模型。"
---

`TencentHunyuanEmbeddings` 类使用腾讯混元 API 为给定文本生成嵌入。

## 设置

1. 在[这里](https://cloud.tencent.com/register)注册腾讯云账户。
2. 在[这里](https://console.cloud.tencent.com/cam/capi)创建 SecretID 和 SecretKey。
3. 将 SecretID 和 SecretKey 分别设置为环境变量 `TENCENT_SECRET_ID` 和 `TENCENT_SECRET_KEY`。

<Tip>
See [this section for general instructions on installing LangChain packages](/oss/langchain/install).
</Tip>

```bash npm
npm install @langchain/community @langchain/core
```
如果你在浏览器环境中使用 LangChain.js，你还需要安装以下依赖：

```bash npm
npm install crypto-js
```

然后确保你按下方所示从 `web` 入口导入。

## 用法

以下是一个示例：

```typescript
// in nodejs environment
import { TencentHunyuanEmbeddings } from "@langchain/community/embeddings/tencent_hunyuan";

// in browser environment
// import { TencentHunyuanEmbeddings } from "@langchain/community/embeddings/tencent_hunyuan/web";

/* Embed queries */
const embeddings = new TencentHunyuanEmbeddings();
const res = await embeddings.embedQuery("你好，世界！");
console.log(res);
/* Embed documents */
const documentRes = await embeddings.embedDocuments(["你好，世界！", "再见"]);
console.log({ documentRes });
```

## 相关链接

- 嵌入模型[概念指南](/oss/integrations/text_embedding)
- 嵌入模型[操作指南](/oss/integrations/text_embedding)
