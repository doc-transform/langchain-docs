---
title: "WolframAlpha 集成"
description: "使用 LangChain JavaScript 集成 WolframAlpha 工具。"
---

WolframAlpha 工具将你的代理和链连接到 WolframAlpha 最先进的计算智能引擎。

## 设置

你需要从 [WolframAlpha 门户](https://developer.wolframalpha.com/)创建一个应用并获取 `appid`。

## 用法

```typescript
import { WolframAlphaTool } from "@langchain/community/tools/wolframalpha";

const tool = new WolframAlphaTool({
  appid: "YOUR_APP_ID",
});

const res = await tool.invoke("What is 2 * 2?");

console.log(res);
```

## 相关

- 工具[概念指南](/oss/langchain/tools)
- 工具[操作指南](/oss/langchain/tools)
