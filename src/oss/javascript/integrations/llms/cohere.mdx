---
title: "Cohere 集成"
description: "使用 LangChain JavaScript 集成 Cohere LLM 模型。"
---

<Warning>
**已弃用**

Cohere 已将其 LLM 的 `generate` 端点标记为弃用。请按照其[迁移指南](https://docs.cohere.com/docs/migrating-from-cogenerate-to-cochat)开始通过 [`ChatCohere`](/oss/integrations/chat/cohere) 集成使用其 Chat API。
</Warning>

本文将帮助你开始使用 Cohere 文本补全模型（LLM）。`Cohere` 所有功能和配置选项的详细文档请参见 [API 参考](https://api.js.langchain.com/classes/langchain_cohere.Cohere.html)。

## 概览

### 集成详情

| Class | Package | Local | Serializable | [PY support](https://python.langchain.com/docs/integrations/llms/cohere) | Downloads | Version |
| :--- | :--- | :---: | :---: |  :---: | :---: | :---: |
| [Cohere](https://api.js.langchain.com/classes/langchain_cohere.Cohere.html) | [@langchain/cohere](https://api.js.langchain.com/modules/langchain_cohere.html) | ❌ | ✅ | ✅ | ![NPM - Downloads](https://img.shields.io/npm/dm/@langchain/cohere?style=flat-square&label=%20&) | ![NPM - Version](https://img.shields.io/npm/v/@langchain/cohere?style=flat-square&label=%20&) |

## 设置

要访问 Cohere 模型，你需要创建 Cohere 账户、获取 API 密钥，并安装 `@langchain/cohere` 集成包。

### 凭据

前往 [cohere.com](https://cohere.com) 注册 Cohere 并生成 API 密钥。完成后设置 `COHERE_API_KEY` 环境变量：

```bash
export COHERE_API_KEY="your-api-key"
```

如果你想获取模型调用的自动追踪，你还可以通过取消下方注释来设置你的 [LangSmith](/langsmith/home) API 密钥：

```bash
# export LANGSMITH_TRACING="true"
# export LANGSMITH_API_KEY="your-api-key"
```

### 安装

LangChain Cohere 集成位于 `@langchain/cohere` 包中：

<CodeGroup>
```bash npm
npm install @langchain/cohere @langchain/core
```
```bash yarn
yarn add @langchain/cohere @langchain/core
```
```bash pnpm
pnpm add @langchain/cohere @langchain/core
```
</CodeGroup>

## 实例化

现在我们可以实例化模型对象并生成对话补全：

```typescript
import { Cohere } from "@langchain/cohere"

const llm = new Cohere({
  model: "command",
  temperature: 0,
  maxTokens: undefined,
  maxRetries: 2,
  // other params...
})
```

### 用于 Azure、AWS Bedrock 和独立 Cohere 实例的自定义客户端

我们可以实例化一个自定义的 `CohereClient` 并将其传递给 ChatCohere 构造函数。

**注意：** 如果提供了自定义客户端，`COHERE_API_KEY` 环境变量和构造函数中的 `apiKey` 参数都将被忽略。

```typescript
import { Cohere } from "@langchain/cohere";
import { CohereClient } from "cohere-ai";

const client = new CohereClient({
  token: "<your-api-key>",
  environment: "<your-cohere-deployment-url>", //optional
  // other params
});

const llmWithCustomClient = new Cohere({
  client,
  // other params...
});
```

## 调用

```typescript
const inputText = "Cohere is an AI company that "

const completion = await llm.invoke(inputText)
completion
```

```text
Cohere is a company that provides natural language processing models that help companies improve human-machine interactions. Cohere was founded in 2019 by Aidan Gomez, Ivan Zhang, and Nick Frosst.
```

---

## API 参考

所有 Cohere 功能和配置的详细文档请参见 [API 参考](https://api.js.langchain.com/classes/langchain_cohere.Cohere.html)。
