---
title: "AI21 集成"
description: "使用 LangChain JavaScript 集成 AI21 LLM 模型。"
---

你可以通过在 [AI21 官网](https://www.ai21.com/)注册 API 密钥来开始使用 AI21Labs 的 Jurassic 系列模型，并查看可用基础模型的完整列表。

以下是在 LangChain.js 中初始化实例的示例：

<Tip>
安装 LangChain 包的通用说明请参见[此章节](/oss/langchain/install)。
</Tip>

```bash npm
npm install @langchain/community @langchain/core
```

```typescript
import { AI21 } from "@langchain/community/llms/ai21";

const model = new AI21({
  ai21ApiKey: "YOUR_AI21_API_KEY", // Or set as process.env.AI21_API_KEY
});

const res = await model.invoke(`Translate "I love programming" into German.`);

console.log({ res });

/*
  {
    res: "\nIch liebe das Programmieren."
  }
 */
```

## 相关链接

- [模型指南](/oss/langchain/models)
