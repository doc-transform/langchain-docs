---
title: "Datadog LLM 可观测性集成"
description: "使用 LangChain JavaScript 集成 Datadog LLM 可观测性回调。"
---

<Warning>
LLM 可观测性功能处于公开测试阶段，API 可能会发生变更。
</Warning>

通过 [Datadog LLM 可观测性](https://docs.datadoghq.com/llm_observability/)，你可以监控、排查和评估你的 LLM 驱动应用程序（如聊天机器人）。你可以调查问题的根本原因，监控运行性能，并评估 LLM 应用的质量、隐私和安全性。

这是一个实验性的社区实现，并非 Datadog 官方支持。它基于 [Datadog LLM 可观测性 API](https://docs.datadoghq.com/llm_observability/api)。

## 安装

<Tip>
安装 LangChain 包的通用说明请参见[此章节](/oss/langchain/install)。
</Tip>

```bash npm
npm install @langchain/community @langchain/core
```

## 用法

```typescript
import { OpenAI } from "@langchain/openai";
import { DatadogLLMObsTracer } from "@langchain/community/experimental/callbacks/handlers/datadog";

/**
 * This example demonstrates how to use the DatadogLLMObsTracer with the OpenAI model.
 * It will produce a "llm" span with the input and output of the model inside the meta field.
 *
 * To run this example, you need to have a valid Datadog API key and OpenAI API key.
 */
export const run = async () => {
  const model = new OpenAI({
    model: "gpt-4",
    temperature: 0.7,
    maxTokens: 1000,
    maxRetries: 5,
  });

  const res = await model.invoke(
    "Question: What would be a good company name a company that makes colorful socks?\nAnswer:",
    {
      callbacks: [
        new DatadogLLMObsTracer({
          mlApp: "my-ml-app",
        }),
      ],
    }
  );

  console.log({ res });
};
```
