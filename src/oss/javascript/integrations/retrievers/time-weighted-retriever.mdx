---
title: "时间加权检索器集成"
description: "使用 LangChain JavaScript 集成时间加权检索器。"
---

时间加权检索器是一种除了考虑相似度外，还考虑时间新近度的检索器。评分算法为：

```typescript
let score = (1.0 - this.decayRate) ** hoursPassed + vectorRelevance;
```

需要注意的是，上面的 `hoursPassed` 指的是检索器中对象上次被访问以来的时间，而不是它被创建以来的时间。这意味着经常被访问的对象保持“新鲜”并获得更高的分数。

`this.decayRate` 是一个可配置的 0 到 1 之间的小数。较低的数值意味着文档将被“记忆”更长时间，而较高的数值则强烈偏向最近访问的文档。

请注意，将衰减率设置为恰好 0 或 1 会使 `hoursPassed` 变得无关紧要，这会使该检索器等同于标准的向量查找。

## 用法

本示例展示了如何使用向量存储初始化 `TimeWeightedVectorStoreRetriever`。
需要注意的是，由于需要元数据，所有文档必须通过**检索器**上的 `addDocuments` 方法添加，而不是向量存储本身。

```typescript
import { TimeWeightedVectorStoreRetriever } from "@langchain/classic/retrievers/time_weighted";
import { MemoryVectorStore } from "@langchain/classic/vectorstores/memory";
import { OpenAIEmbeddings } from "@langchain/openai";

const vectorStore = new MemoryVectorStore(new OpenAIEmbeddings());

const retriever = new TimeWeightedVectorStoreRetriever({
  vectorStore,
  memoryStream: [],
  searchKwargs: 2,
});

const documents = [
  "My name is John.",
  "My name is Bob.",
  "My favourite food is pizza.",
  "My favourite food is pasta.",
  "My favourite food is sushi.",
].map((pageContent) => ({ pageContent, metadata: {} }));

// All documents must be added using this method on the retriever (not the vector store!)
// so that the correct access history metadata is populated
await retriever.addDocuments(documents);

const results1 = await retriever.invoke("What is my favourite food?");

console.log(results1);

/*
[
  Document { pageContent: 'My favourite food is pasta.', metadata: {} }
]
 */

const results2 = await retriever.invoke("What is my favourite food?");

console.log(results2);

/*
[
  Document { pageContent: 'My favourite food is pasta.', metadata: {} }
]
 */
```

## 相关

- [检索指南](/oss/langchain/retrieval)
