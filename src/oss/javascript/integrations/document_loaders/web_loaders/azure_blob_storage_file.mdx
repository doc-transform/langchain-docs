---
title: "Azure Blob Storage 文件集成"
description: "使用 LangChain JavaScript 集成 Azure Blob Storage 文件文档加载器。"
---

<Tip>
**兼容性**

仅在 Node.js 中可用。
</Tip>

本文介绍如何将 Azure 文件加载到 LangChain 文档中。

## 设置

要使用此加载器，你需要已经设置好 Unstructured 并在可用的 URL 端点准备就绪。它也可以配置为在本地运行。

有关如何操作的信息，请参见[此处](/oss/integrations/document_loaders/file_loaders/unstructured)的文档。

你还需要安装官方的 Azure Storage Blob 客户端库：

```bash npm
npm install @langchain/community @langchain/core @azure/storage-blob
```

## 用法

配置好 Unstructured 后，你可以使用 Azure Blob Storage File 加载器来加载文件，然后将其转换为文档。

```typescript
import { AzureBlobStorageFileLoader } from "@langchain/community/document_loaders/web/azure_blob_storage_file";

const loader = new AzureBlobStorageFileLoader({
  azureConfig: {
    connectionString: "",
    container: "container_name",
    blobName: "example.txt",
  },
  unstructuredConfig: {
    apiUrl: "http://localhost:8000/general/v0/general",
    apiKey: "", // this will be soon required
  },
});

const docs = await loader.load();

console.log(docs);
```
