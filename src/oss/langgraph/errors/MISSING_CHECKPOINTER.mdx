---
title: MISSING_CHECKPOINTER
---

你正在尝试使用 LangGraph 内置的持久化功能，但未提供 checkpointer。

:::python
这发生在 @[`StateGraph`][StateGraph] 或 @[`@entrypoint`] 的 `compile()` 方法中缺少 `checkpointer` 时。
:::

:::js
这发生在 @[`StateGraph`][StateGraph] 或 @[`entrypoint`] 的 `compile()` 方法中缺少 `checkpointer` 时。
:::


## 故障排除

以下方法可能有助于解决此错误：

:::python
-   初始化并将 checkpointer 传递给 @[`StateGraph`][StateGraph] 或 @[`@entrypoint`] 的 `compile()` 方法。
:::
:::js
-   初始化并将 checkpointer 传递给 @[`StateGraph`][StateGraph] 或 @[`entrypoint`] 的 `compile()` 方法。
:::

:::python

```python
from langgraph.checkpoint.memory import InMemorySaver
checkpointer = InMemorySaver()

# Graph API
graph = StateGraph(...).compile(checkpointer=checkpointer)

# Functional API
@entrypoint(checkpointer=checkpointer)
def workflow(messages: list[str]) -> str:
    ...
```

:::

:::js

```typescript
import { InMemorySaver, StateGraph } from "@langchain/langgraph";
const checkpointer = new InMemorySaver();

// Graph API
import { StateGraph } from "@langchain/langgraph";
const graph = new StateGraph(...).compile({ checkpointer });

// Functional API
import { entrypoint } from "@langchain/langgraph";
const workflow = entrypoint(
    { checkpointer, name: "workflow" },
    async (messages: string[]) => {
        // ...
    }
);
```

:::

-   使用 LangGraph API，这样你无需手动实现或配置 checkpointer。API 会为你处理所有持久化基础设施。

## 相关

- 阅读更多关于[持久化](/oss/langgraph/persistence)的内容。
