---
title: 记忆概述
---



[记忆](/oss/langgraph/add-memory)是一个记住先前交互信息的系统。对于 AI Agent 来说，记忆至关重要，因为它让它们能够记住先前的交互、从反馈中学习并适应用户偏好。随着 Agent 处理更复杂的任务和大量用户交互，这种能力对于效率和用户满意度都变得至关重要。

本概念指南涵盖两种类型的记忆，基于其召回范围：

* [短期记忆](#short-term-memory)，或[线程](/oss/langgraph/persistence#threads)作用域记忆，通过在会话内维护消息历史来跟踪正在进行的对话。LangGraph 将短期记忆作为 Agent [状态](/oss/langgraph/graph-api#state)的一部分管理。状态通过 [checkpointer](/oss/langgraph/persistence#checkpoints) 持久化到数据库，以便线程可以在任何时候恢复。短期记忆在图被调用或某个步骤完成时更新，状态在每个步骤开始时读取。
* [长期记忆](#long-term-memory)跨会话存储用户特定或应用级别的数据，并在对话线程*之间*共享。它可以*在任何时候*和*在任何线程中*被召回。记忆的作用域是任何自定义命名空间，不仅仅是单个线程 ID。LangGraph 提供 [store](/oss/langgraph/persistence#memory-store)（[参考文档](https://langchain-ai.github.io/langgraph/reference/store/#langgraph.store.base.BaseStore)）让你保存和召回长期记忆。

![Short vs long](/oss/images/short-vs-long.png)

## 短期记忆

[短期记忆](/oss/langgraph/add-memory#add-short-term-memory)让你的应用能够记住单个[线程](/oss/langgraph/persistence#threads)或对话中的先前交互。[线程](/oss/langgraph/persistence#threads)将会话中的多次交互组织在一起，类似于电子邮件将消息分组在单个对话中。

LangGraph 将短期记忆作为 Agent 状态的一部分管理，通过线程作用域的检查点持久化。此状态通常可以包括对话历史以及其他有状态数据，如上传的文件、检索的文档或生成的工件。通过将这些存储在图的状态中，bot 可以访问给定对话的完整上下文，同时保持不同线程之间的隔离。

### 管理短期记忆

对话历史是最常见的短期记忆形式，而长对话对当今的 LLM 构成了挑战。完整的历史可能无法容纳在 LLM 的上下文窗口中，导致不可恢复的错误。即使你的 LLM 支持完整的上下文长度，大多数 LLM 在长上下文上的表现仍然不佳。它们会被过时或偏离主题的内容“分心”，同时还会受到更慢的响应时间和更高的成本影响。

聊天模型使用消息接受上下文，包括开发者提供的指令（系统消息）和用户输入（人类消息）。在聊天应用中，消息在人类输入和模型响应之间交替，产生一个随时间增长的消息列表。由于上下文窗口有限且富含 token 的消息列表成本高昂，许多应用可以从使用手动移除或遗忘过时信息的技术中受益。

![Filter](/oss/images/filter.png)

关于管理消息的常见技术的更多信息，请参见[添加和管理记忆](/oss/langgraph/add-memory#manage-short-term-memory)指南。

## 长期记忆

LangGraph 中的[长期记忆](/oss/langgraph/add-memory#add-long-term-memory)允许系统在不同的对话或会话之间保留信息。与**线程作用域**的短期记忆不同，长期记忆保存在自定义的“命名空间”中。

长期记忆是一个复杂的挑战，没有一套适用于所有情况的解决方案。然而，以下问题提供了一个框架，帮助你理解不同的技术：

* 记忆的类型是什么？人类使用记忆来记住事实（[语义记忆](#semantic-memory)）、经历（[情景记忆](#episodic-memory)）和规则（[程序性记忆](#procedural-memory)）。AI Agent 也可以以相同的方式使用记忆。例如，AI Agent 可以使用记忆来记住关于用户的特定事实以完成任务。
* [何时更新记忆？](#writing-memories)记忆可以作为 Agent 应用逻辑的一部分更新（即“在热路径上”）。在这种情况下，Agent 通常在响应用户之前决定记住事实。或者，记忆可以作为后台任务更新（在后台/异步运行并生成记忆的逻辑）。我们在[下方部分](#writing-memories)解释了这些方法之间的权衡。

不同的应用需要不同类型的记忆。虽然类比并不完美，但研究[人类记忆类型](https://www.psychologytoday.com/us/basics/memory/types-of-memory?ref=blog.langchain.dev)可能很有启发性。一些研究（如 [CoALA 论文](https://arxiv.org/pdf/2309.02427)）甚至将这些人类记忆类型映射到 AI Agent 使用的记忆类型。

| 记忆类型 | 存储内容 | 人类示例 | Agent 示例 |
|-------------|----------------|---------------|---------------|
| [语义记忆](#semantic-memory) | 事实 | 在学校学到的东西 | 关于用户的事实 |
| [情景记忆](#episodic-memory) | 经历 | 我做过的事情 | 过去的 Agent 操作 |
| [程序性记忆](#procedural-memory) | 指令 | 本能或运动技能 | Agent 系统提示词 |

### 语义记忆

[语义记忆](https://en.wikipedia.org/wiki/Semantic_memory)，无论在人类还是 AI Agent 中，都涉及对特定事实和概念的保留。在人类中，它可以包括在学校学到的信息以及对概念及其关系的理解。对于 AI Agent，语义记忆通常用于通过记住过去交互中的事实或概念来个性化应用。

<Note>
语义记忆不同于"语义搜索"——语义搜索是一种使用"含义"（通常是嵌入向量）查找相似内容的技术。语义记忆是心理学术语，指存储事实和知识；而语义搜索是基于含义而非精确匹配来检索信息的方法。
</Note>

#### 档案

语义记忆可以通过不同方式管理。例如，记忆可以是一个持续更新的"档案"，包含关于用户、组织或其他实体（包括 Agent 自身）的范围明确且具体的信息。档案通常只是一个 JSON 文档，包含你选择的用于表示领域的各种键值对。

记忆档案时，你需要确保每次都在**更新**档案。因此，你需要传入之前的档案并[要求模型生成新的档案](https://github.com/langchain-ai/memory-template)（或生成某些 [JSON patch](https://github.com/hinthornw/trustcall) 应用到旧档案上）。随着档案变大，这可能容易出错，可能需要将档案拆分为多个文档，或在生成文档时使用**严格**解码以确保记忆 schema 保持有效。

![Update profile](/oss/images/update-profile.png)

#### 集合

或者，记忆可以是一个随时间持续更新和扩展的文档集合。每条单独的记忆可以范围更窄、更容易生成，这意味着你随时间**丢失**信息的可能性更小。对于 LLM 来说，为新信息生成*新*对象比将新信息与现有档案协调更容易。因此，文档集合往往会导致[更高的下游召回率](https://en.wikipedia.org/wiki/Precision_and_recall)。

然而，这将一些复杂性转移到了记忆更新上。模型现在必须*删除*或*更新*列表中的现有项目，这可能很棘手。此外，某些模型可能默认过度插入，而其他模型可能默认过度更新。参见 [Trustcall](https://github.com/hinthornw/trustcall) 包了解一种管理方式，并考虑评估（例如使用 [LangSmith](/langsmith/evaluate-chatbot-tutorial) 等工具）来帮助你调整行为。

使用文档集合还将复杂性转移到了列表上的记忆**搜索**。`Store` 目前支持[语义搜索](https://langchain-ai.github.io/langgraph/reference/store/#langgraph.store.base.SearchOp.query)和[内容过滤](https://langchain-ai.github.io/langgraph/reference/store/#langgraph.store.base.SearchOp.filter)。

最后，使用记忆集合可能使得向模型提供全面的上下文变得困难。虽然单条记忆可能遵循特定的 schema，但这种结构可能无法捕获记忆之间的完整上下文或关系。因此，当使用这些记忆生成响应时，模型可能缺少在统一档案方法中更容易获得的重要上下文信息。

![Update list](/oss/images/update-list.png)

无论采用哪种记忆管理方式，核心要点是 Agent 将使用语义记忆来[锚定其响应](https://python.langchain.com/docs/concepts/rag/)，这通常会带来更加个性化和相关的交互。

### 情景记忆

[情景记忆](https://en.wikipedia.org/wiki/Episodic_memory)，无论在人类还是 AI Agent 中，都涉及对过去事件或行为的回忆。[CoALA 论文](https://arxiv.org/pdf/2309.02427)很好地描述了这一点：事实可以写入语义记忆，而*经历*可以写入情景记忆。对于 AI Agent，情景记忆通常用于帮助 Agent 记住如何完成任务。

:::python
在实践中，情景记忆通常通过[少样本示例提示](/langsmith/create-few-shot-evaluators)来实现，Agent 从过去的序列中学习以正确执行任务。有时"展示"比"告知"更容易，而 LLM 从示例中学习效果很好。少样本学习让你通过使用输入-输出示例更新提示词来["编程"](https://x.com/karpathy/status/1627366413840322562)你的 LLM，以展示预期行为。虽然可以使用各种[最佳实践](https://python.langchain.com/docs/concepts/#1-generating-examples)来生成少样本示例，但挑战通常在于根据用户输入选择最相关的示例。
:::

:::js
在实践中，情景记忆通常通过少样本示例提示来实现，Agent 从过去的序列中学习以正确执行任务。有时"展示"比"告知"更容易，而 LLM 从示例中学习效果很好。少样本学习让你通过使用输入-输出示例更新提示词来["编程"](https://x.com/karpathy/status/1627366413840322562)你的 LLM，以展示预期行为。虽然可以使用各种最佳实践来生成少样本示例，但挑战通常在于根据用户输入选择最相关的示例。
:::

:::python
请注意，记忆 [store](/oss/langgraph/persistence#memory-store) 只是将数据存储为少样本示例的一种方式。如果你想要更多开发者参与，或将少样本与评估框架更紧密地结合，你还可以使用 [LangSmith Dataset](/langsmith/manage-datasets) 来存储数据，并实现自己的检索逻辑来根据用户输入选择最相关的示例。

参见这篇[博客文章](https://blog.langchain.dev/few-shot-prompting-to-improve-tool-calling-performance/)展示了使用少样本提示改进工具调用性能，以及这篇[博客文章](https://blog.langchain.dev/aligning-llm-as-a-judge-with-human-preferences/)使用少样本示例将 LLM 与人类偏好对齐。
:::

:::js
请注意，记忆 [store](/oss/langgraph/persistence#memory-store) 只是将数据存储为少样本示例的一种方式。如果你想要更多开发者参与，或将少样本与评估框架更紧密地结合，你还可以使用 LangSmith Dataset 来存储数据，并实现自己的检索逻辑来根据用户输入选择最相关的示例。

参见这篇[博客文章](https://blog.langchain.dev/few-shot-prompting-to-improve-tool-calling-performance/)展示了使用少样本提示改进工具调用性能，以及这篇[博客文章](https://blog.langchain.dev/aligning-llm-as-a-judge-with-human-preferences/)使用少样本示例将 LLM 与人类偏好对齐。
:::

### 程序性记忆

[程序性记忆](https://en.wikipedia.org/wiki/Procedural_memory)，无论在人类还是 AI Agent 中，都涉及记住执行任务时使用的规则。在人类中，程序性记忆就像内化的任务执行知识，例如通过基本运动技能和平衡来骑自行车。情景记忆则涉及回忆特定经历，例如第一次不用辅助轮成功骑自行车，或一次令人难忘的风景骑行。对于 AI Agent，程序性记忆是模型权重、Agent 代码和 Agent 提示词的组合，它们共同决定了 Agent 的功能。

在实践中，Agent 修改其模型权重或重写代码是相当罕见的。然而，Agent 修改自己的提示词则更为常见。

一种有效的改进 Agent 指令的方法是通过["反思"](https://blog.langchain.dev/reflection-agents/)或元提示。这涉及用当前指令（如系统提示词）以及最近的对话或明确的用户反馈来提示 Agent。然后 Agent 根据这些输入改进自己的指令。这种方法对于那些难以预先指定指令的任务特别有用，因为它允许 Agent 从交互中学习和适应。

例如，我们构建了一个 [Tweet 生成器](https://www.youtube.com/watch?v=Vn8A3BxfplE)，使用外部反馈和提示词重写来为 Twitter 生成高质量的论文摘要。在这种情况下，具体的摘要提示词很难*预先*指定，但用户可以很容易地批评生成的 Tweet 并提供如何改进摘要过程的反馈。

以下伪代码展示了如何使用 LangGraph 记忆 [store](/oss/langgraph/persistence#memory-store) 来实现这一点，使用 store 保存提示词，`update_instructions` 节点获取当前提示词（以及从与用户对话中捕获的 `state["messages"]` 反馈），更新提示词，并将新提示词保存回 store。然后，`call_model` 从 store 获取更新后的提示词并使用它来生成响应。

:::python
```python
# Node that *uses* the instructions
def call_model(state: State, store: BaseStore):
    namespace = ("agent_instructions", )
    instructions = store.get(namespace, key="agent_a")[0]
    # Application logic
    prompt = prompt_template.format(instructions=instructions.value["instructions"])
    ...

# Node that updates instructions
def update_instructions(state: State, store: BaseStore):
    namespace = ("instructions",)
    instructions = store.search(namespace)[0]
    # Memory logic
    prompt = prompt_template.format(instructions=instructions.value["instructions"], conversation=state["messages"])
    output = llm.invoke(prompt)
    new_instructions = output['new_instructions']
    store.put(("agent_instructions",), "agent_a", {"instructions": new_instructions})
    ...
```
:::

:::js
```typescript
// Node that *uses* the instructions
const callModel: GraphNode<typeof State> = async (state, config) => {
  const namespace = ["agent_instructions"];
  const instructions = await config.store?.get(namespace, "agent_a");
  // Application logic
  const prompt = promptTemplate.format({
    instructions: instructions[0].value.instructions
  });
  // ...
};

// Node that updates instructions
const updateInstructions: GraphNode<typeof State> = async (state, config) => {
  const namespace = ["instructions"];
  const currentInstructions = await config.store?.search(namespace);
  // Memory logic
  const prompt = promptTemplate.format({
    instructions: currentInstructions[0].value.instructions,
    conversation: state.messages
  });
  const output = await llm.invoke(prompt);
  const newInstructions = output.new_instructions;
  await store.put(["agent_instructions"], "agent_a", {
    instructions: newInstructions
  });
  // ...
};
```
:::

![Update instructions](/oss/images/update-instructions.png)

### 写入记忆

Agent 写入记忆有两种主要方法：["在热路径中"](#in-the-hot-path)和["在后台"](#in-the-background)。

![Hot path vs background](/oss/images/hot_path_vs_background.png)

#### 在热路径中

在运行时创建记忆既有优势也有挑战。积极方面是，这种方法允许实时更新，使新记忆可以立即用于后续交互。它还能实现透明性，因为可以在创建和存储记忆时通知用户。

然而，这种方法也带来了挑战。如果 Agent 需要一个新工具来决定将什么提交到记忆中，可能会增加复杂性。此外，推理要保存什么到记忆的过程可能影响 Agent 延迟。最后，Agent 必须在记忆创建和其他职责之间多任务处理，可能影响创建的记忆的数量和质量。

例如，ChatGPT 使用 [save_memories](https://openai.com/index/memory-and-new-controls-for-chatgpt/) 工具以内容字符串形式插入/更新记忆，在每条用户消息时决定是否以及如何使用此工具。参见我们的 [memory-agent](https://github.com/langchain-ai/memory-agent) 模板作为参考实现。

#### 在后台

将记忆创建为单独的后台任务有几个优势。它消除了主应用中的延迟，将应用逻辑与记忆管理分离，并允许 Agent 更专注地完成任务。这种方法还在记忆创建的时间安排上提供了灵活性，以避免冗余工作。

然而，这种方法有其自身的挑战。确定记忆写入的频率变得至关重要，因为不频繁的更新可能使其他线程缺少新的上下文。决定何时触发记忆形成也很重要。常见策略包括在设定的时间段后调度（如果有新事件发生则重新调度）、使用 cron 计划，或允许用户或应用逻辑手动触发。

参见我们的 [memory-service](https://github.com/langchain-ai/memory-template) 模板作为参考实现。

### 记忆存储

LangGraph 将长期记忆作为 JSON 文档存储在 [store](/oss/langgraph/persistence#memory-store) 中。每条记忆组织在自定义的 `namespace`（类似文件夹）和独特的 `key`（类似文件名）下。命名空间通常包含用户或组织 ID 或其他标签，使信息更容易组织。这种结构实现了记忆的层次化组织。然后通过内容过滤器支持跨命名空间搜索。

:::python
```python
from langgraph.store.memory import InMemoryStore


def embed(texts: list[str]) -> list[list[float]]:
    # Replace with an actual embedding function or LangChain embeddings object
    return [[1.0, 2.0] * len(texts)]


# InMemoryStore saves data to an in-memory dictionary. Use a DB-backed store in production use.
store = InMemoryStore(index={"embed": embed, "dims": 2})
user_id = "my-user"
application_context = "chitchat"
namespace = (user_id, application_context)
store.put(
    namespace,
    "a-memory",
    {
        "rules": [
            "User likes short, direct language",
            "User only speaks English & python",
        ],
        "my-key": "my-value",
    },
)
# get the "memory" by ID
item = store.get(namespace, "a-memory")
# search for "memories" within this namespace, filtering on content equivalence, sorted by vector similarity
items = store.search(
    namespace, filter={"my-key": "my-value"}, query="language preferences"
)
```
:::

:::js
```typescript
import { InMemoryStore } from "@langchain/langgraph";

const embed = (texts: string[]): number[][] => {
  // Replace with an actual embedding function or LangChain embeddings object
  return texts.map(() => [1.0, 2.0]);
};

// InMemoryStore saves data to an in-memory dictionary. Use a DB-backed store in production use.
const store = new InMemoryStore({ index: { embed, dims: 2 } });
const userId = "my-user";
const applicationContext = "chitchat";
const namespace = [userId, applicationContext];

await store.put(
  namespace,
  "a-memory",
  {
    rules: [
      "User likes short, direct language",
      "User only speaks English & TypeScript",
    ],
    "my-key": "my-value",
  }
);

// get the "memory" by ID
const item = await store.get(namespace, "a-memory");

// search for "memories" within this namespace, filtering on content equivalence, sorted by vector similarity
const items = await store.search(
  namespace,
  {
    filter: { "my-key": "my-value" },
    query: "language preferences"
  }
);
```
:::

关于记忆 store 的更多信息，请参见[持久化](/oss/langgraph/persistence#memory-store)指南。
